# Parlamento App V1
Prototipo funcional mobile-first. Inicio, vencimientos, agenda, tareas y navegación. `supabase.sql` contiene el esquema inicial para persistencia real.

## Ejecutar
npm install
npm run dev

## Siguiente paso
Crear proyecto gratuito en Supabase, ejecutar `supabase.sql` y conectar el frontend a Supabase Auth/DB.
